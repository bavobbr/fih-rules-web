import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.fieldhockeyrules.agent",
  appName: "Field Hockey Rules Agent",
  webDir: "dist",
};

export default config;
